<div class="tool_info">
  <p>Tool: <%= @tool.name %></p>
  <p>Price: <%= @tool.price %></p>
  <p>Quantity: <%= @tool.quantity %></p>
  <p>Category: <%= @tool.category.name %></p>

</div>
<%= link_to "Edit", edit_user_tool_path(current_user, @tool) %>
<%= link_to "Delete", user_tool_path(current_user, @tool), method: :delete %>
<p><%= link_to " << Back to Tool List!", user_tools_path(current_user) %></p>
